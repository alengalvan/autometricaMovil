import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alen.autometrica',
  appName: 'autometrica',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
